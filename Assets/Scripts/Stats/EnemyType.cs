namespace Cursor.Stats
{
    /// <summary>
    /// Enemy type identifiers. Color-coded per GDD:
    /// A = Blue, B = Green, C = Red, D = Purple.
    /// </summary>
    public enum EnemyType
    {
        A,
        B,
        C,
        D,
    }
}
