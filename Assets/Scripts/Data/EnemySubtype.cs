namespace Cursor.Data
{
    /// <summary>
    /// Geometric subtypes for enemies. Each subtype modifies base stats via multipliers.
    /// </summary>
    public enum EnemySubtype
    {
        Triangle,
        Square,
        Hexagon,
        Circle,
    }
}
