namespace Cursor.Pool
{
    /// <summary>
    /// Identifiers for object pools managed by ObjectPoolManager.
    /// </summary>
    public enum PoolKey
    {
        Enemy,
        Collectable,
    }
}
